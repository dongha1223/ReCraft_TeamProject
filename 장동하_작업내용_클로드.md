# 장동하 작업내용 (Claude 정리)

대화 종료 시점에 Claude가 직접 요약·누적 기록합니다.

---

## 2026-06-01 (일) - 버그 수정, UI 기능, GoldChest 시스템

### 완료 항목

#### 1. 스테이지 전환 시 아이템 사라짐 버그 수정
- `RewardSpawner.SpawnRewards()` — `Instantiate` 시 부모를 `transform`으로 지정 → 스테이지 루트 비활성화 시 아이템도 함께 사라짐

#### 2. 체력 강화 시 현재 체력 연동
- `EnhanceUIController.TryConfirmUpgrade()` — MaxHp 강화 전후 delta 계산 후 `PlayerStats.Heal(delta)` 호출 → 강화량만큼 현재 체력도 증가

#### 3. 인벤토리 최대 9개 제한
- `InventoryService` — `MaxCapacity = 9`, `IsFull` 프로퍼티 추가, `Add()`에서 초과 시 무시
- `ItemPickup.OnInteract()` — `IsFull` 체크 후 픽업 차단

#### 4. 인벤토리 & HUD 초상화 동적 갱신
- `InventoryController` — `FormManager.OnFormSwapped` 구독, `job-slot` 배경 이미지 폼 교체 시 갱신
- `PlayerHPUI` — `portrait-circle` 캐싱, 폼 교체 시 초상화 즉시 갱신
- `WarriorForm.asset` — `_icon`에 기사 얼굴.png 연결

#### 5. 스타트 씬 리뉴얼
- `StartScreen.uss` — 배경 `Logo.jpg` → `배경.png` 교체
- `StartScreen.uxml` / `StartScreen.uss` — "SACRAMENT OF CONFESSION" 황금색 타이틀 추가 (top 35%, 52px, letter-spacing 4px)

#### 6. GoldChest 시스템 구축
- `GoldChestSpawner.cs` 신규 생성 — `RewardSpawner`와 동일 구조, `OnAllEnemiesDead` 구독
- `Start()`에서 1회 `Instantiate` 후 `SetActive`로 재사용 (Destroy 없는 크기 1 풀)
- `GoldChest.cs` — `Init(goldAmount)` 추가, `OnEnable()`에서 `_opened=false` + `Animator.Rebind()` 리셋
- 씬 9개 스테이지(Stage2~6, MidBoss, MidNPC, Boss, Boss2)에 `GoldChestSpawner` 배치
- 각 스테이지 아이템 스폰 포인트 최소 X보다 1.5 왼쪽에 배치
- `GoldChestPickup.cs` 삭제 (기존 `GoldChest.cs`로 통합)

#### 7. 최적화
- `InventoryController` — `RefreshSynergyPanel()` HashSet/List 재사용 버퍼화, `RarityColor()` static readonly 캐싱, `OnFormSwapped` next 파라미터 직접 사용
- `EnhanceUIController` — `statSvc` 로컬 변수로 null 체크 중복 제거
- `ItemPickup` — `OnFocused()` new Color → `static readonly _highlightColor`
- `RewardSpawner` — `ResolveSpawnPoints()` 결과 `_resolvedSpawnPoints`에 캐싱 (`??=`)
- `GoldChestSpawner` — `_spawnedChest` 추적으로 재활성화 시 `SetActive(false)` 재사용

---

## 2026-05-31 (토) - 최적화, 버그 수정, 기능 추가

### 완료 항목

#### 1. 어제 코드 최적화
- BossHeadLaserPattern.cs: 들여쓰기 버그 수정, `IDamageable` 캐싱, `RotatePivot()` null 체크 루프 밖으로
- BossHeadFinalPhase.cs: `RisePlatforms()` 매 프레임 Vector3 할당 제거 (startPositions 배열 미리 계산)

#### 2. 플랫폼 수정
- Platform_LL, Platform_LR 삭제 (하단 2개 제거)
- 남은 4개(HL/HR/ML/MR)에 `PlatformEffector2D` + `usedByEffector=true` 추가 → 아래서 점프 통과 가능
- 6개 모두 레이어 Ground(9) → Platform(10) 변경

#### 3. 화염구 낙하 패턴 구현 (BossFireballPattern)
- `BossFireballDataSO.cs` — 파라미터 SO (SpawnY=12, FallSpeed=3, BallCount=5, SpawnInterval=1, HitDamage=40, DebrisDamage=3, DebrisDuration=4, PatternCooldown=7)
- `FireballProjectile.cs` — FixedUpdate + rb.MovePosition 낙하, OverlapBoxNonAlloc 플레이어 감지, Ground 착지→잔해, 잔해 틱뎀 4초
- `BossFireballPattern.cs` — 5개 1초 간격 스폰, BossCustomPatternBase 구현
- `BossHeadFinalPhase.cs` — 레이저 활성 후 FireballLoop 코루틴 병렬 실행
- `FireballProjectile.prefab` / `BossFireballData.asset` — 전부 자동 연결

#### 4. 화염구 버그 수정 및 재최적화
- 플레이어 히트 미감지: Kinematic RB2D vs Dynamic RB2D → OnTriggerEnter2D 미발생. FixedUpdate OverlapBoxNonAlloc으로 직접 체크로 교체
- 크기 2배, 플랫폼 통과, 착지 Y -0.4 보정, `_debrisCheckSize` / `debrisPos` 캐시

#### 5. 상점 UI (EnhanceUI) 개선
- `EnhanceUI.uxml` — 용기 행을 물리 공격력+스킬 공격력 좌우 분할(.upgrade-row-split), 강인함→체력 개명
- `EnhanceUI.uss` — 분할 레이아웃 클래스, 아이콘 3개 CSS (#icon-courage=Attack, #icon-skill=Skill_Attack, #icon-health=Health)
- `EnhanceNode_SkillAttack0.asset` / `EnhanceNode_Health0.asset` — nodeId skill_0/health_0, SkillPower/MaxHp, 비용·% 동일
- 씬 `_nodeEntries` 자동 연결

#### 6. 미드보스 표지판 버그 수정
- 원인: `Stage_MidBoss.asset stageType=Boss` → `SignpostController`가 `BossController.OnBossDead` 구독 (미발행)
- 수정: `RefreshSubscription()`을 `StageManager.OnAllEnemiesDead` 단일 경로로 통일
- `_isBossStage` 필드 제거, `SetIsBossStage()` 빈 메서드화

#### 7. 보스 이름/종족 시스템
- `BossMainDataSO` / `BossControllerDataSO` — `BossName` / `BossRace` 필드 추가
- `BossMainController` / `BossController` — 프로퍼티 노출
- `BossHUDController` — `_nameLabel` / `_raceLabel` 쿼리, `SetBossLabels()` 추가
- UXML 기존 `boss-name-label` / `boss-race-label` 자동 연동

#### 8. 보스2 파이널 페이즈 사망 버그 수정
- 원인: `StageBossRoot2.prefab` `_parts[3] = null` (Boss_left 미연결) → Head 사망 시 HandleBossDeath() 미호출
- 수정: `_parts[3]`에 Boss_left BossPartHP fileID(`2962540320998201448`) 연결
- `BossHeadFinalPhase.HandleBossDead()` — `_headTransform.gameObject.SetActive(false)` 추가
- `BossRoomManager.HandleBossDead()` — ClearOverrideTarget + RestoreOffset 추가

#### 9. 체력 관련 버그 2건 수정
- `PlayerStats.Heal()`: `_maxHp` 필드 → `MaxHp` 프로퍼티 (모디파이어 반영값으로 상한 계산)
- `PlayerStats.Start()`: 코루틴 1프레임 지연 후 `_currentHp = MaxHp` (EnhanceUI 모디파이어 적용 후 읽기)

#### 10. 오늘 코드 최적화
- PlayerStats: `Debug.Log` 2개 제거, `using System.Collections` 추가, `IEnumerator` 단순화
- SignpostController: `_isBossStage` 필드 제거, Start() 조건 간소화

---

## 2026-03-30

### 설정
- 대화 요약 파일 생성 (`장동하_작업내용_클로드.md`)
- 매 대화 마무리 시 Claude가 이 파일에 내용을 누적 정리하기로 결정

### 시작화면 구현
- `SampleScene` → `InGameScene` 이름 변경 (이미 `Assets/InGameScene.unity`로 존재)
- `Assets/Scenes/StartScene.unity` 씬 생성, 빌드 세팅 등록 (StartScene:0, InGameScene:1)
- `Assets/UI/StartScreen.uxml` / `StartScreen.uss` 생성 (UIToolkit 기반)
- `Assets/Scripts/UI/StartSceneController.cs` 생성
  - 검은화면 Fade In → 로고 이미지 + "아무 키나 입력하세요" 점멸
  - 키보드 / 마우스 좌클릭 입력 감지
  - Fade Out → InGameScene 씬 전환
- `Assets/Screenshots/Logo.jpg` 이미지를 로고로 사용 (USS 상대경로 `../Screenshots/Logo.jpg`)
- `Assets/UI/StartScreenPanelSettings.asset` 생성 (HUDPanelSettings 복제)

### 남은 사항
- InGameScene 진입 시 Fade In 효과 (미구현, 필요 시 추가)
- 로고를 이미지 파일 대신 직접 제작한 에셋으로 교체 예정

---

## 2026-03-31

### 시작화면 보완
- 로고 이미지(`Assets/Screenshots/Logo.jpg`)를 화면 전체 배경으로 변경
  - `logo-image` VisualElement를 content-root 밖으로 분리, position absolute 전체화면
  - USS: `stretch-to-fill`로 화면 꽉 채움
- "아무 키나 입력하세요" 위치를 화면 하단 중앙으로 변경 (bottom: 80px)
- 마우스 좌클릭 입력 감지 추가 (`Mouse.current.leftButton.wasPressedThisFrame`)

### 일시정지 메뉴 구현
- `Assets/UI/PauseMenu.uxml` / `PauseMenu.uss` 생성
  - 반투명 오버레이 + 어두운 보라색 패널 + 해골(☠) + "일시정지" 타이틀
  - 메뉴 5개: 돌아가기, 새 게임, 컨트롤, 설정, 게임종료
  - 선택 항목 강조: `menu-item--selected` CSS 클래스 동적 적용
- `Assets/Scripts/UI/PauseMenuController.cs` 생성
  - ESC: 메뉴 열기/닫기 + `Time.timeScale` 0/1 제어
  - ↑↓ 화살표: 순환 네비게이션 (`% MenuCount`)
  - Enter/Space: 선택 실행
  - 게임종료만 기능 구현 (`Application.Quit` / 에디터는 `isPlaying = false`)
- InGameScene에 `PauseMenuController` GameObject 배치 완료

### 남은 사항
- 새 게임 / 컨트롤 / 설정 기능 미구현 (빈 케이스로 대기 중)
- InGameScene 진입 시 Fade In 효과 미구현

---

## 2026-04-01

### 3스테이지 시스템 구현 (단일 씬 방식)

**구조 설계**
- 씬을 새로 만드는 대신 InGameScene 안에서 `##--STAGES--##` 부모 오브젝트 아래 Stage1Root / Stage2Root / Stage3Root를 두고 SetActive로 전환하는 방식 채택
- Stage2, Stage3는 에디터에서 비활성 상태로 시작

**스테이지 구성**
- Stage 1: NPC존 (적 없음, 표지판만 존재 — 추후 상점/강화 공간 예정)
- Stage 2: 기존 맵 (오크 + 마법사 적 혼합)
- Stage 3: 신규 맵 (Stage 2 지형 복제 기반, 오크 + 마법사 혼합)

**생성한 파일**

| 파일 | 역할 |
|------|------|
| `Assets/Scripts/Core/StageManager.cs` | 스테이지 전환·적 카운트 총괄 싱글턴 |
| `Assets/Scripts/Core/FadeManager.cs` | UIToolkit 기반 FadeIn/FadeOut·게임클리어 UI |
| `Assets/Scripts/Stage/SignpostController.cs` | 표지판 상호작용 (알파·F키·이벤트 구독) |
| `Assets/Scripts/Stage/EnemySpawner.cs` | 스테이지 활성화 시 적 등록 및 재방문 복구 |
| `Assets/UI/GameClearUI.uxml` / `.uss` | GAME CLEAR 오버레이 + 카운트다운 레이블 |

**핵심 로직**
- `EnemySpawner.OnEnable`: 스테이지 루트 활성화 시 자동 호출 → 하위 EnemyStats 전부 찾아 `ResetStats()` + `RegisterEnemy()`
- `EnemyStats.OnDead()`: 사망 시 `StageManager.OnEnemyDied()` 호출 → 카운터 감소
- `StageManager.ActivateStage()`: `_aliveEnemyCount = 0` 초기화를 `SetActive(true)` **전**에 실행 (순서 중요)
- `CheckAllEnemiesDeadNextFrame()`: SetActive 직후 한 프레임 대기 후 AllEnemiesDead 검사 → 이벤트 발행
- `SignpostController`: `OnAllEnemiesDead` 이벤트 구독 → 수신 시 알파 50→255, F키 프롬프트 활성화

**게임 클리어 흐름**
- Stage 3 표지판 `_isLastStage = true` 설정
- F키 → `TriggerGameClear()` → FadeOut → GAME CLEAR 표시 → FadeIn → 10초 카운트다운 → FadeOut → Stage 1 복귀 → FadeIn

**해결한 버그**
1. 게임 시작 시 Stage 3에서 시작하는 버그
   → `GameObject.Find()`는 비활성 오브젝트를 탐색 못 함. Stage2·3를 에디터에서 비활성화하여 해결
2. 표지판이 적 처치 전에 활성화되는 버그
   → `CheckAllEnemiesDeadNextFrame` 코루틴으로 다음 프레임에 검사하도록 수정
3. Stage 1→2 전환 시 오류 (UnassignedReferenceException)
   → Inspector에서 직접 할당 후 해결, 불필요한 자동 탐색 코드 전부 제거

### 몬스터 카운터 UI 추가

- `PlayerHUD.uxml`에 `enemy-counter` 요소 추가 (☠ 레이블 + 숫자 레이블)
- `PlayerHUD.uss`에 스타일 추가 — 미니맵 왼쪽(right: 210px, bottom: 20px), 반투명 패널, 해골 빨간색/숫자 흰색
- `StageManager.cs`에 `public int AliveEnemyCount` 프로퍼티 추가
- `PlayerHPUI.cs`에 `UpdateEnemyCount()` 추가 — 매 프레임 `StageManager.AliveEnemyCount`를 읽어 레이블 갱신

### 남은 사항
- Stage 1 NPC존 콘텐츠 미구현 (상점·강화 등 추후 추가 예정)

---

## 2026-04-04

### 일시정지 메뉴 서브창 구현 (새 게임 확인 / 컨트롤 / 설정)

**새 게임 확인 다이얼로그**
- `PauseMenu.uxml`에 `confirm-overlay` 추가 (예 / 아니오 버튼)
- `PauseMenuController.cs`에 확인 다이얼로그 로직 추가
  - 새 게임 선택 → 다이얼로그 오픈, ←→ 방향키로 예/아니오 전환
  - 예 → `StageManager.Instance.RestartGame()` (FadeOut → PlayerStats.FullRestore → Stage1 복귀 → FadeIn)
  - `StageManager.cs`에 `RestartGame()` / `DoRestart()` 추가
  - `PlayerStats.cs`에 `FullRestore()` 추가

**컨트롤 창**
- `PauseMenu.uxml`에 `controls-overlay` 추가 (7×2 키 바인딩 그리드 + 초기화 + 돌아가기)
- `Assets/UI/ControlsMenu.uss` 생성
- `Assets/Scripts/UI/ControlsMenuController.cs` 생성
  - ↑↓ 행 이동, ←→ 컬럼 전환 (바인딩 행에서만), Enter/Space 실행
  - 키 리바인딩은 추후 `InputActionRebindingExtensions`로 구현 예정

**설정 창**
- `PauseMenu.uxml`에 `settings-overlay` 추가 (그래픽/데이터/오디오/게임플레이 섹션)
- `Assets/UI/SettingsMenu.uss` 생성 (UIToolkit `<ui:Slider>` 스타일 오버라이드 포함)
- `Assets/Scripts/UI/SettingsMenuController.cs` 생성
  - 슬라이더 5개 (화면흔들림·진동·음량·음악·효과음): 마우스 클릭/드래그 + 방향키 ±0.1
  - UIToolkit 이벤트가 Unity 6 + New Input System 환경에서 미동작 → `Mouse.current` + 수동 Y반전 좌표 변환으로 해결

**ESC 우선순위 체계**
- ESC 처리를 `PauseMenuController.Update()` 상단에 집중
- 컨트롤 → 설정 → 확인 → 포즈 메뉴 순으로 닫기

### 마우스 지원 전체 추가

- PauseMenu 5개 항목, 확인 예/아니오, 컨트롤 14행+초기화+돌아가기, 설정 15행+돌아가기 전부 마우스 hover/click 지원
- 좌표 변환: `new Vector2(sp.x, Screen.height - sp.y)` (UIToolkit Y축 반전 수동 처리)

### 코드 최적화

- 마우스 상태(`Mouse.current.position`, `leftButton.*`) 3개 컨트롤러에서 각자 읽던 것을 `PauseMenuController`에서 1회 읽고 서브 컨트롤러에 전달하는 구조로 변경
- `SetConfirmSelection`의 `Label[]` 매 호출 힙 할당 → `_confirmBtns` 필드 캐싱
- `ControlsMenuController.ClearHighlight`: col 무관하게 양쪽 RemoveFromClassList → col에 맞는 쪽만 제거
- `SettingsMenuController.HandleRowMouseInput` 루프 내 불필요한 null 체크 제거
- `HandleSliderMouseInput` → `HandleMouseInput` 리네임 + 상단 주석 수정

### 버그 수정

- 컨트롤/설정 창에서 키보드로 "돌아가기" 선택 시 창이 닫히지 않고 맨 위로 이동하는 버그
  - 원인: MonoBehaviour Update() 실행 순서 미보장 → 서브 컨트롤러가 먼저 실행되어 닫힌 후, PauseMenuController가 같은 Enter 키 입력으로 재오픈
  - 수정: `PauseMenuController`에 `[DefaultExecutionOrder(-10)]` 추가 → 항상 서브 컨트롤러보다 먼저 실행

### 남은 사항
- 설정 창 각 항목 기능 미구현 (슬라이더 값 실제 적용, 해상도/화면모드 전환 등)
- 컨트롤 창 키 리바인딩 미구현

---

## 2026-04-08

### 인벤토리 UI 구현 (Tab 키)

**구조 설계**
- 별도 GameObject(`InventoryUI`) + 별도 UIDocument + 별도 PanelSettings로 구성 (PauseMenu와 완전 독립)
- Tab 키로 열기/닫기, 열리는 동안 `Time.timeScale = 0` 일시정지
- ESC 우선순위: 인벤토리 → 하위 창 → 포즈 메뉴 순으로 닫기 (`PauseMenuController`에 `_inventoryController` SerializeField 추가)

**생성 파일**

| 파일 | 역할 |
|------|------|
| `Assets/UI/InventoryUI.uxml` | 3단 레이아웃 (시너지 / 직업+정수+아이템 / 상세정보) |
| `Assets/UI/InventoryUI.uss` | 갈색 RPG 톤 스타일 (전체 1230×750px) |
| `Assets/Scripts/UI/InventoryController.cs` | 인벤토리 UI 총괄 컨트롤러 |
| `Assets/UI/InventoryPanelSettings.asset` | PauseMenuPanelSettings 복제 생성 |

**UI 구성 (왼쪽 → 오른쪽)**

- **시너지 패널**: 각인 슬롯 최대 6개 고정 (UXML), 장착 아이템 기준 1개 이상 활성화된 각인만 표시
  - 카드 레이아웃: `[아이콘] [현재개수(30px 큰 글자)] [이름 / 2→4 티어텍스트]`
  - `EquipmentService.OnItemEquipped/Unequipped` 이벤트 구독 → 자동 갱신
- **가운데 패널**: 직업(95×95px, Player_Pic.png 초상화) / 정수(95×95px 빈칸) / 아이템 3×3 그리드(9칸)
- **오른쪽 패널**: 아이템 미선택 시 빈칸, 선택 시 아이콘(원형 123px) + 레어도(색상별) + 이름 + 설명 + 시너지 2개 + 자세히 버튼(기능 미구현)

**마우스 클릭 처리**
- UIToolkit `ClickEvent` 대신 `Mouse.current.leftButton.wasPressedThisFrame` + `worldBound.Contains(panelPos)` 방식 사용 (PauseMenuController와 동일 패턴)
- 같은 슬롯 재클릭 시 선택 해제

**레어도 색상**
- 일반: 흰색 / 희귀: 파란색 / 고유: 주황색 / 전설: 노란색

**직업 초상화**
- `[SerializeField] Texture2D _jobPortrait` → 인스펙터에서 `Assets/Screenshots/Player_Pic.png` 연결

---

## 2026-04-07

### 표지판 [F] 상호작용 버그 수정

**문제**
- 적이 0마리인 상태에서 표지판�� 접근해도 [F] 프롬프트가 뜨지 않고 다음 스테이지로 이동도 안 됨

**원인 분석 (3중 복합 버그)**
1. **`PlayerInteractor` 컴포넌트가 Player에 없었음** — 핵심 원인. 플레이어가 상호작용 탐지 자체를 전혀 안 하고 있었음
2. **씬의 Signpost 3개 모두 Collider2D 없음** — `Physics2D.OverlapCircle`이 탐지 불가
3. **`OverlapCircleNonAlloc` 사용** — Unity 6에서 deprecated. ContactFilter2D 없이 호출 시 일부 환경에서 트리거 콜라이더 미감지

**수정 내용**
- `Player` GameObject에 `PlayerInteractor` 컴포넌트 추가 (씬 저장)
- `Signpost`, `Signpost_Copy` × 2 에 `BoxCollider2D (isTrigger=true, size 1×1)` 추가 (씬 저장)
- `PlayerInteractor.FindClosest()`: `Physics2D.OverlapCircleNonAlloc` → `Physics2D.OverlapCircle(ContactFilter2D)` 교체
  - `ContactFilter2D.useTriggers = true`, `useLayerMask = false` (모든 레이어 포함)
- 거리 비교: `Vector2.Distance` (sqrt) → `sqrMagnitude` 로 교체 (매 프레임 불필요한 sqrt 제거)

---

---

## 2026-04-09

### 컨트롤 창 키 리바인딩 전면 구현

**신규 파일**

| 파일 | 역할 |
|------|------|
| `Assets/Scripts/Core/KeyBindingService.cs` | 13개 액션 키 바인딩 전역 관리, PlayerPrefs 저장/불러오기 |

**핵심 설계**
- `KeyBindingService`는 static 클래스. `Action` enum(MoveUp/Down/Left/Right, Inventory, Interact, Attack, Jump, Dash, Skill1, Skill2, Essence, Swap) + 방향키대쉬(bool 토글)
- 기본값: ↑↓←→, Tab, F, X, Space, Z, A, S, D, C
- `WasPressedThisFrame` / `IsPressed` / `WasReleasedThisFrame` / `ToDisplayString` 헬퍼 제공
- 게임 실행 중 리바인딩 즉시 반영 (static 필드 공유)

**`ControlsMenuController.cs` 전면 재작성**
- 각 키 뱃지 Label(`ctrl-left-{i}-key`, `ctrl-right-{i}-key`) 캐싱 → `Open()` 시 현재 바인딩으로 갱신
- 항목 선택(Enter/Space/클릭) → "새 키 누르기" 오버레이 표시 → 다음 키 입력(ESC 제외)으로 바인딩 변경
- **쌍방 스왑**: 새 키가 이미 다른 액션에 할당된 경우 두 액션의 키를 서로 교체
- 방향키대쉬(row 5, 왼쪽 열): 리바인딩 대신 Enter/Space/◄► 으로 켜짐/꺼짐 토글
- 초기화 → `KeyBindingService.ResetToDefaults()` + 전체 라벨 갱신

**UXML / CSS**
- `PauseMenu.uxml`: `controls-overlay` 내부에 `rebind-panel` 추가 (`position: absolute` 전체 커버)
- `ControlsMenu.uss`: `.rebind-panel` / `.rebind-box` / `.rebind-title` 스타일 추가 (금색 테두리, "새 키 누르기" 텍스트)

**플레이어 스크립트 전체 KeyBindingService로 전환**

| 파일 | 변경 내용 |
|------|-----------|
| `PlayerController.cs` | leftArrow/rightArrow/space/downArrow → KeyBindingService |
| `PlayerDash.cs` | Z키, 방향키대쉬 체크 → KeyBindingService |
| `PlayerAttack.cs` | X키 → KeyBindingService |
| `PlayerSkill.cs` | A/S키 → KeyBindingService |
| `PlayerInteractor.cs` | F키 3종(pressed/held/released) → KeyBindingService |
| `InventoryController.cs` | Tab키 → KeyBindingService |

### 버그 수정

- **ESC 중복 처리 버그**: 컨트롤/설정 창 Enter로 진입 시 서브메뉴에서 한 번 클릭되는 현상 → `_openedThisFrame` 플래그로 열린 프레임 입력 무시 (`ControlsMenuController`, `SettingsMenuController` 양쪽에 적용)
- **일시정지 중 TAB 인벤토리 열림**: `Time.timeScale > 0f` 조건 추가
- **리바인딩 중 ESC 처리**: `PauseMenuController`가 `_controlsMenu.IsRebinding` 확인 후 창 닫기 스킵 → `ControlsMenuController`가 직접 취소 처리

### 최적화

| 파일 | 최적화 내용 |
|------|-------------|
| `ControlsMenuController` | `Start()`에서 유효 Key 사전 필터링(`_validKeys`) → 리바인딩 루프 내 `try/catch` 완전 제거 |
| `PlayerDash` | 감속 루프 진입 전 `DirectionalDash` + 키 컨트롤 캐시 → 루프 내 KeyBindingService 호출 0회 |
| `PlayerInteractor` | `HandleHoldInput`에서 키 컨트롤 한 번만 획득 → 프레임당 3→1회 조회로 감소 |

### 남은 사항
- 설정 창 각 항목 기능 미구현 (슬라이더 실제 적용, 해상도/화면모드 등)

---

### NPC 대화 시스템 구현

**신규 파일**

| 파일 | 역할 |
|------|------|
| `Assets/Scripts/NPC/DialogueData.cs` | NPC 이름·대사 라인 배열을 담는 ScriptableObject |
| `Assets/Scripts/NPC/NPCController.cs` | IInteractable 구현, [F] 프롬프트 및 대화 시작 |
| `Assets/Scripts/UI/DialogueUIController.cs` | 대화 UI 싱글턴, 시네마틱 바·패널 트랜지션 총괄 |
| `Assets/UI/DialogueUI.uxml` | 상단 바 / 하단 바 / 대화 패널 (NPC 이름 + 대사 + 버튼 2개) |
| `Assets/UI/DialogueUI.uss` | 시네마틱 검은 바 트랜지션, 대화 패널 스타일, 버튼 선택 강조 |
| `Assets/UI/DialoguePanelSettings.asset` | sortingOrder: 200 (HUD 위) |

**핵심 동작**
- NPC 반경 접근 시 [F] 텍스트(TextMesh, Signpost 방식과 동일) 활성화
- F키 → 상단/하단 검은 바 슬라이드인(0.4s) → 대화 패널 페이드인(0.25s)
- 키보드 ↑↓로 "대화"/"취소" 버튼 선택, Enter/Space/F 실행, ESC 취소
- `DialogueUIController.IsActive` 동안 이동·점프·공격·스킬·대시 전부 차단
- ESC 우선순위: 대화창 닫기 → 포즈 메뉴 (겹치지 않음)
- 대화 중 포즈 메뉴 열기 차단, 포즈 중 F키 상호작용 차단

**ShopGirl 프리팹**
- `Assets/Sprites/ShopGirl/ShopGirl_Idle.anim`: 5프레임 8fps 루프
- `Assets/Sprites/ShopGirl/ShopGirlAnimator.controller`: Idle 단일 상태
- `Assets/Prefabs/ShopGirl.prefab`: SpriteRenderer + Animator + BoxCollider2D(trigger) + NPCController
  - 자식 FKeyPrompt: TextMesh "[F]  대화하기", 기본 비활성

**레이아웃 상세**
- 상단 바: height 0 → 80px 트랜지션
- 하단 바: height 0 → 190px 트랜지션
- 대화 패널: left/right 80px 여백(중앙 기준 좁힘), npc-name 20px / 대사 18px / 버튼 20px
- `WaitForSeconds` static 캐싱으로 GC 할당 방지

### 타이프라이터 효과 추가 (`DialogueUIController.cs`)

- 대사 글자를 순서대로 출력 (초당 40자, Inspector에서 조절 가능)
- 매 프레임 경과 시간 누적 방식 — `WaitForSeconds` 없이 부드럽게 동작
- 타이핑 중 확인 키(Enter/Space/F) 누르면 전체 텍스트 즉시 표시(스킵)
- 스킵 후 한 번 더 누르면 다음 줄로 이동

### 입력 차단 전면 정리

**`UIState.cs` 신규 생성** (`Assets/Scripts/Core/`)
```
UIState.IsBlockingInput = DialogueUIController.IsActive || PauseMenuController.IsPaused || InventoryController.IsOpen
```
- 새 UI 추가 시 이 파일 한 곳만 수정하면 됨

**`InventoryController.IsOpen` static 전환**
- 기존 인스턴스 프로퍼티 → `public static bool IsOpen`
- `PauseMenuController`의 인스턴스 접근도 타입 접근으로 수정

**입력 차단 적용 스크립트**

| 스크립트 | 차단 조건 |
|----------|-----------|
| `PlayerAttack` | `UIState.IsBlockingInput` |
| `PlayerSkill` | `UIState.IsBlockingInput` |
| `PlayerDash` | `UIState.IsBlockingInput` (기존 `DialogueUIController.IsActive`만 → 확장) |
| `PlayerInteractor` | `UIState.IsBlockingInput` (기존 `PauseMenuController.IsPaused`만 → 확장) |
| `TagController` | `UIState.IsBlockingInput` (신규 추가) |
| `InventoryController` | 대화/포즈 중 TAB 열기 차단 (신규 추가) |
| `PlayerController.HandleMovement` | `UIState.IsBlockingInput` (포즈/인벤 차단 누락 수정) |
| `PlayerController.HandleJump` | `UIState.IsBlockingInput` (포즈/인벤 차단 누락 수정) |

### 코드 최적화 (`PlayerController.cs`)

- `FeetBoxSize` 프로퍼티(`new Vector2` 매 프레임 생성) → `Awake`에서 `_feetBoxSize` 필드로 캐싱
- `DrawDebugGizmos()` 제거 — `OnDrawGizmos()`와 중복 (`Gizmos.DrawWireCube`로 이미 커버)

---

---

## 2026-04-14

### 스테이지 SO 구조 설계 및 드랍 시스템 구현

**설계 결정사항**
- 스테이지 데이터를 ScriptableObject로 관리 (고정 100층, 수동 제작 방식)
- 몬스터는 맵 프리팹에 직접 배치 → SO에 몬스터 종류 불필요
- 클리어 보상 아이템은 UI 선택창이 아닌 **포탈 옆 월드 스폰** 방식

**신규 파일**

| 파일 | 역할 |
|------|------|
| `Assets/Scripts/Stage/StageDataSO.cs` | 스테이지 단위 데이터 SO (씬이름·타입·테마·보상·BGM) |
| `Assets/Scripts/Stage/StageListSO.cs` | 전체 100층 StageDataSO 배열 컨테이너 |
| `Assets/Scripts/Core/Items/Data/ItemDatabaseSO.cs` | 전체 아이템 정의 마스터 목록 |
| `Assets/Scripts/Stage/DropSystem.cs` | 테마 기반 가중치 랜덤 드랍 정적 유틸리티 |
| `Assets/Scripts/Stage/RewardSpawner.cs` | 적 전멸 시 포탈 옆 아이템 스폰 |

**열거형 / 구조체**
- `StageType`: Start / Normal / Shop / Boss
- `MapTheme`: None / Pride / Greed / Anger / Sloth / Envy / Gluttony / Lust (7대 죄악)
- `RewardData` (인라인 struct): `itemChoiceCount`, `bonusGold`

**ItemDefinition 추가 필드**
- `float baseDropWeight` — 드랍 가중치 (높을수록 확률 상승)
- `MapTheme[] dropThemes` — 비어있거나 None만 있으면 전 테마 공용 아이템

**드랍 로직 (`DropSystem.RollDrops`)**
1. `dropThemes`가 null/빈/None만 → 공용 아이템으로 분류
2. 현재 스테이지 `MapTheme` 일치 아이템 + 공용 아이템 필터링
3. `baseDropWeight` 기반 가중치 랜덤, 중복 없이 `itemChoiceCount`개 반환

**`RewardSpawner` 동작**
- `StageManager.OnAllEnemiesDead` 구독 (SignpostController와 동일 패턴)
- `_spawnPoints` 배열 비어있거나 전부 null이면 `##--REWARD_SPAWNS--##` 자식에서 자동 탐색
- `ItemPickup.Init(ItemDefinition)` 메서드 추가 → 런타임 스폰 후 아이콘/데이터 주입

**Stage2Root 세팅**
- `##--REWARD_SPAWNS--##` 자식 그룹 + SpawnPoint_Item_0~2 추가 (Signpost 왼쪽)
- `Assets/Prefabs/ItemPickup.prefab` 신규 생성 (SpriteRenderer + BoxCollider2D + ItemPickup)
- `Assets/Scripts/Stage/StageSO/Stage_01_02.asset`: stageType=Normal, mapTheme=Pride, itemChoiceCount=3

**버그 수정**
- `dropThemes: 00000000` 문제 — Inspector가 빈 배열 대신 None(0)을 직렬화해 필터 탈락
  → `DropSystem.BuildPool`에서 None만 있는 배열은 공용으로 처리하도록 수정
  → 기존 에셋 2개 (`glassSword`, `BurningBody`) `dropThemes: []`로 정리
- `_spawnPoints [null, null, null]` 직렬화 문제 — MCP로 Transform 배열 참조 저장 불가
  → `ResolveSpawnPoints()`에서 null이 아닌 요소가 없으면 계층에서 자동 탐색하도록 수정

**디버그 진행 중**
- 맵 전환 안 되는 현상 조사 중
- `SignpostController.OnInteract`와 `HandleAllEnemiesDead`에 임시 Debug.Log 추가
- 콘솔 출력 확인 후 원인 파악 예정

### 남은 사항
- 맵 전환 버그 원인 파악 및 수정
- SignpostController 디버그 로그 제거 (버그 수정 후)
- ItemDatabase에 실제 아이템 등록 (현재 2개만 등록)
- 아이템 `dropThemes` 설정 (테마별 전용 아이템 분류)

---

---

## 2026-04-28

### StageManager 전면 리팩토링

**변경 전**: `_stageRoots[]`, `_spawnPoints[]`, `StageListSO` 세 배열 분리 관리  
**변경 후**: `StageEntry` 단일 구조체 배열 `_stages[]` — 데이터·루트·스폰포인트를 한 곳에서 관리

- `StageEntry` struct: `StageDataSO data`, `GameObject root`, `Transform spawnPoint`
- `CurrentStageData` 프로퍼티 추가 (현재 스테이지 SO 외부 접근용)
- 배열 순서 = 게임 진행 순서 (순서 변경만으로 스테이지 구성 변경 가능)

### 마지막 스테이지 표지판 자동 감지

- `SignpostController._isLastStage`를 Inspector에서 수동 설정하던 방식 제거
- `ActivateStage(index)`에서 `index == _stages.Length - 1` 조건으로 자동 판단
- `SignpostController.SetIsLastStage(bool)` 런타임 주입 메서드 추가
- `_isLastStage` Tooltip에 "StageManager가 런타임에 자동 주입 — 직접 수정 불필요" 표기

### StageShopRoot 생성 (NPC 스테이지)

InGameScene `##--STAGES--##` 아래 신규 스테이지 루트 추가

| 오브젝트 | 위치 | 설정 |
|---------|------|------|
| Ground | (0,0,0) | scale (20,1,1), Layer 9 |
| Platform_Upper | (0,4.5,0) | scale (14,1,1), Layer 10 + PlatformEffector2D |
| SpawnPoint_Shop | (-8,1,0) | — |
| ShopGirl (프리팹) | (2,1.2,0) | scale (-0.3,0.3,0.3) |
| Signpost_Copy | (7.3,1.022,0) | — |
| NPC_01 | (-3,0.5,0) | scale (-0.3,0.3,0.3) |

- `Stage_Shop_01.asset` 생성 (stageType=Shop, mapTheme=None)
- `StageList.asset`에 4번째 항목으로 등록

### NPC_01 추가

- `Assets/Screenshots/NPC_01.png` 스프라이트 시트 (5프레임) 사용
- `Assets/Sprites/NPC_01/NPC_01_Idle.anim` — 8fps, 루프, bottom-center pivot
- `Assets/Sprites/NPC_01/NPC_01_Animator.controller` — Idle 단일 상태
- `Assets/Scripts/Core/Items/NpcSO/NPC_01_Dialogue.asset` — DialogueData SO 생성
- StageShopRoot/NPC_01: `NPCController` + `BoxCollider2D(trigger)` + FKeyPrompt 자식
- FKeyPrompt localPos (-0.048, 4.3, 0) — 캐릭터 머리 위 표시

### Platform_Upper 플랫폼 전환

- `PlatformEffector2D` 컴포넌트 추가
- `BoxCollider2D.usedByEffector = true`
- Layer 10 (Platform) — 아래에서 위로 통과 가능한 발판

### 최적화 (StageManager, SignpostController)

**StageManager**
- `Awake`에서 `PlayerStats`, `FormSkillController`, `PlayerDash`, `Rigidbody2D` 캐싱 → 재시작마다 `GetComponent` 4회 제거
- `CacheSignposts()`: 각 스테이지 `SignpostController`를 배열에 미리 캐싱 → 스테이지 전환마다 `GetComponentInChildren` 제거
- `_aliveEnemyBuffer` 재사용 리스트 → `GetAliveEnemyTransforms()` 호출마다 `new List<>()` GC 할당 제거

**SignpostController**
- `OnInteract`, `HandleAllEnemiesDead`의 `Debug.Log` → `#if UNITY_EDITOR` 래핑 (빌드에서 제거)

### 남은 사항
- `NPC_01_Dialogue.asset` _npcName / _lines 실제 내용 Inspector에서 입력 필요

---

### 스테이지 복귀 시 플레이어 상태 초기화

**문제**
- 게임 클리어 후 1스테이지로 복귀할 때 체력·스킬 쿨타임·대시 충전량이 초기화되지 않음

**원인**
- `DoGameClear()`에 플레이어 초기화 코드 자체가 없었음
- `DoRestart()`는 HP만 초기화하고 스킬·대시는 누락

**수정 내용**

| 파일 | 추가 내용 |
|------|-----------|
| `PlayerSkill.cs` | `ResetSkills()` — 진행 중 코루틴 중단, 쿨타임 0 리셋, 롤링 상태·회전 초기화 |
| `PlayerDash.cs` | `ResetDash()` — 충전량 복구, 대시·쿨타임 상태 초기화, 중력 원복, 고스트 이펙트 정리 |
| `StageManager.cs` | `ResetPlayer()` 헬퍼 추가 (HP·스킬·대시 일괄 초기화), `DoGameClear()`와 `DoRestart()` 양쪽에서 호출 |

---

### NPC 선택지 대화 시스템 구현

**설계 방향**
- `DialogueData(SO)` + `NPCController` + `DialogueUIController` 세 파일 수정으로 확장
- NPC별 전용 클래스 대신 공통 구조 + 컴포넌트 조합 방식 채택

**수정 파일**

| 파일 | 변경 내용 |
|------|-----------|
| `Assets/Scripts/NPC/DialogueData.cs` | `_hasChoice`, `_yesResponse`, `_noResponse` 필드 추가 (Inspector Header: "선택지") |
| `Assets/Scripts/NPC/NPCController.cs` | `_onYesChosen`, `_onNoChosen` UnityEvent 추가 → `StartDialogue` 콜백으로 전달 |
| `Assets/Scripts/UI/DialogueUIController.cs` | 선택 분기 처리 전면 추가 (아래 상세) |

**`DialogueUIController` 변경 상세**
- `StartDialogue(data, onYes, onNo)` — 콜백 인자 추가
- `_showingResponse` / `_currentResponseText` 상태 추가
- `OnContinueClicked`: 마지막 줄 도달 시 HasChoice면 `onYes` 호출 → 반응 문구 표시
- `OnCancelClicked`: HasChoice면 타이핑 중 스킵, 이후 `onNo` 호출 → 반응 문구 표시
- 마지막 질문 줄에서 버튼 텍스트 "예"/"아니오"로 자동 전환, 반응 표시 후 "대화"/"취소" 원복
- `CloseSequence`에서 콜백·반응 상태 전부 초기화

**신규 파일**

| 파일 | 역할 |
|------|------|
| `Assets/Scripts/NPC/NPCHealEffect.cs` | `_onYesChosen` 이벤트에 연결해 플레이어 체력 회복 |

- `[SerializeField] float _healAmount = 50f` — Inspector에서 수치 조절
- `PlayerStats` 첫 호출 시 캐싱 → 이후 `FindObjectOfType` 재호출 없음

**Inspector 설정 방법**
1. `NPC_01_Dialogue.asset` — `Has Choice` 체크, `Yes Response: 여기있어!`, `No Response: 먹어줘 ㅠㅠ`
2. NPC_01 GameObject에 `NPCHealEffect` 컴포넌트 추가, `Heal Amount: 50`
3. `NPCController._onYesChosen` → `NPCHealEffect.Execute` 연결

**최적화**
- `OnCancelClicked` `HasChoice` 이중 체크 → 단일 분기로 병합
- `StopTypewriterIfRunning()` 헬퍼 추출 → `ShowCurrentLine`, `ShowResponse`, `SkipTypewriter` 재사용
- `_npcNameLabel.text` → `OpenSequence`에서 한 번만 설정 (대화 중 불변)
- `NPCHealEffect._playerStats` 캐싱

---

## 2026-05-04

### 강화 UI 전반 개선

**대화창 입력 차단 버그 수정**
- 강화 UI가 열려 있을 때 F·Enter·방향키가 동작하지 않던 버그 수정
- 원인: `DialogueUIController.Update()`에서 `EnhanceUIController.IsOpen`이면 모든 키 처리를 `return`했음
- 수정: ESC만 차단(`if (kb.escapeKey.wasPressedThisFrame && !EnhanceUIController.IsOpen)`)하고 나머지 입력은 정상 처리

**강화 UI 위치 조정 (`EnhanceUI.uss`)**
- `#enhance-panel`: `right: 80px → 140px` — 오른쪽에 초상화 공간 확보

**강화 UI Z-order 수정 (`EnhancePanelSettings.asset`)**
- `m_SortingOrder: 200 → 300` — 시네마틱 검은 바(DialoguePanelSettings sortingOrder:200)보다 위에 렌더링
- 원인: 같은 PanelSettings 내 UIDocument `sortingOrder`는 효과 없음. 서로 다른 PanelSettings 간에는 asset의 `m_SortingOrder`로 제어해야 함

**강화 UI 항목명 변경 (`EnhanceUI.uxml`)**
- VisualElement 이름: `row-skull` → `row-courage`, `skull-node-*` → `courage-node-*`
- VisualElement 이름: `row-bone` → `row-endurance`, `bone-node-*` → `endurance-node-*`
- 카테고리 레이블 텍스트: "두개골" → "용기", "뼈" → "강인함"
- 카테고리 아이콘: 보라색 인라인 배경 → `신념_아이콘.png` (USS `background-image: url()` 방식)

**강화 비용 색상·아이콘 통일 (`EnhanceUI.uss`)**
- `#info-cost-icon`: `-unity-background-image-tint-color: rgb(225, 185, 70)` 추가 (신념 패널 노란색과 일치)
- `#info-cost-amount`: `color: rgb(225, 185, 70)`, `margin-right: 6px`, `-unity-text-align: middle-left` 정렬 추가

**샵걸 초상화 추가 (`EnhanceUI.uxml` / `EnhanceUI.uss`)**
- `#portrait-panel` 오른쪽에 `#portrait-image` 추가
- USS: `width: 320px; height: 740px; background-image: url('../Screenshots/샵걸_이미지.png'); bottom: 15px` (시네마틱 바 윗선과 맞닿도록)
- 스크립트 없이 순수 USS로 이미지 표시 (CLAUDE.md 규칙 준수)

### 보유한 신념 패널 추가 (`DialogueUI.uxml` / `DialogueUI.uss`)

- 강화 UI가 열릴 때만 보이는 "보유한 신념" 패널 추가
- `dialogue-root` 레벨에 `dialogue-panel`과 형제 요소로 배치 (대화 패널 내부 아님 — 분리 배경)
- `position: absolute; bottom: 0; left: 80px; width: 130px; height: 175px`으로 대화창 왼쪽에 독립 배치
- 항상 레이아웃 공간 차지 (`opacity: 0` 기반) → 대화창 크기 변화 없음
- `#belief-icon`: `신념_아이콘.png` (36×36), `#belief-title`, `#belief-count` (28px 노란색)
- `EnhanceUIController.Open()` → `ShowBeliefPanel(true)`, `HidePanel()` → `ShowBeliefPanel(false)`

### 대화창·강화 UI·신념 패널 동시 Fade-out

- `CloseSequence()` 첫 yield **이전**에 `ShowBeliefPanel(false)` + `EnhanceUIController.FadeOutWithDialogue()` 동시 호출
- `FadeOutWithDialogue()`: `IsOpen = false` + `panel-visible` 제거만 (대화 시스템 건드리지 않음)
- 세 패널이 같은 프레임에 CSS opacity 트랜지션 시작 → 동시 사라짐 연출

### 최적화

| 파일 | 내용 |
|------|------|
| `StageManager.cs` | `WaitForSeconds(1f)` 반복 생성 → `_waitOneSecond` static 캐싱; 코루틴 내 `FadeManager.Instance` 로컬 변수로 캐싱 |
| `NPCController.cs` | `_onYesChosen.Invoke` / `_onNoChosen.Invoke` 메서드 그룹 → `Awake()`에서 `_yesAction`, `_noAction` 델리게이트로 캐싱 |
| `InventoryService.cs` | `Clear()` 메서드 추가 |
| `EquipmentService.cs` | `Clear()` 메서드 추가 (역순 Unequip으로 이벤트 발행) |

### Dead Code 제거 (`EnhanceUIController.cs`)

- `[SerializeField] private Sprite _portraitSprite` 제거 — 항상 null, 미사용
- `private VisualElement _portraitImage` 제거 — dead code 전용 필드
- `Start()`의 `_portraitImage = root.Q<VisualElement>("portrait-image")` 제거
- `if (_portraitSprite != null) _portraitImage.style.backgroundImage = new StyleBackground(_portraitSprite)` 제거 (CLAUDE.md 런타임 스프라이트 생성 금지 위반 코드)

### 남은 사항
- 강화 노드 실제 기능 구현 (선택 시 능력치 상승 등) — 추후 추가 예정

---

## 2026-05-05 ~ 05-06

### 신념(Belief) 메타 화폐 시스템

**신규 파일**

| 파일 | 역할 |
|------|------|
| `Assets/Scripts/Core/BeliefManager.cs` | 신념 재화 싱글턴. DontDestroyOnLoad, PlayerPrefs 저장 |
| `Assets/Scripts/Core/EnhanceSaveService.cs` | 강화 레벨 PlayerPrefs 저장/불러오기 정적 유틸리티 |
| `Assets/Scripts/Core/EnhanceNodeDefinition.cs` | 강화 노드 데이터 ScriptableObject (비용·스탯 모디파이어 정의) |
| `Assets/EnhanceNodes/EnhanceNode_Courage0.asset` | 용기 0번 노드: 물리 공격력 +10%/레벨, 최대 5레벨, 비용 1·3·7·15·30 |

**BeliefManager 핵심**
- Normal 타입 스테이지 클리어 시 누적 횟수 × 1씩 신념 획득 (1회=+1, 2회=+2, ...)
- 보스 스테이지 제외
- Inspector에서 `_totalBelief` 직접 수정 가능 → 플레이 중 즉시 UI 반영 (`OnValidate`에서 `OnBeliefChanged` 이벤트 발행, `PlayerPrefs.Save()` 호출 안 함)
- `StageManager.OnNormalStageCleared` / `OnRunRestarted` 이벤트 구독 (캐시된 참조로 `OnDestroy` 안전 해지)

**StageManager 이벤트 추가**
- `public event Action OnNormalStageCleared` — Normal 스테이지 클리어 시 발행
- `public event Action OnRunRestarted` — 재시작·게임클리어 후 1스테이지 복귀 시 발행
- `TransitionToNextStage()`, `TriggerGameClear()`: stageType == Normal일 때만 `OnNormalStageCleared` 발행

### 강화 UI 기능 전면 구현

**EnhanceUIController.cs 전면 재작성**

| 기능 | 내용 |
|------|------|
| `NodeEntry` struct | `uiName`(UXML name) + `EnhanceNodeDefinition` 쌍 |
| 노드 VisualElement 캐싱 | `Start()`에서 `_nodeElements[]` / `_nodeLevelLabels[]` 배열로 캐싱 — 클릭/갱신마다 DOM 재쿼리 없음 |
| `_selectedNodeIndex` | 이전 선택 노드만 O(1) 클래스 제거 (전체 순회 불필요) |
| `Open()` | 신념 패널 표시, 버튼 "강화"/"나가기"로 전환, 노드 레벨 갱신, 정보 패널 초기화 |
| `TryConfirmUpgrade()` | 레벨 확인 → 신념 차감 → 저장 → 스탯 모디파이어 적용 → UI 갱신 → NPC 피드백 |
| `ResetAllUpgrades()` | 모든 노드 레벨 0 초기화, 스탯 모디파이어 전부 제거, `SetLevelNoFlush` 일괄 처리 후 `FlushSave` 1회 |
| `ClearSelection()` | 선택 해제 공통 헬퍼 (`HidePanel`, `ResetAllUpgrades`, `Open`에서 재사용) |
| `ApplyModifiers()` | `RemoveAllModifiersFromSource` 후 레벨 수 × 스펙으로 모디파이어 재적용 (Multiply: 1 + level × vpl) |

**강화 결과 NPC 피드백**
- 강화 성공 → `"강화되었어요!"`, 신념 부족 → `"신념이 부족해!"`, 초기화 → `"강화가 초기화되었어요."` 대화창에 타이프라이터로 표시
- `DialogueUIController.ShowEnhanceFeedback(text)` — `_showingResponse` 등 대화 상태 변경 없이 텍스트만 교체 → 강화 UI 유지한 채 계속 강화 가능
- 피드백 문구는 Inspector에서 수정 가능 (`_successFeedback`, `_failFeedback`, `_resetFeedback`)

**레벨 프리뷰 수정 (`EnhanceNodeDefinition.GetNextLevelPreview`)**
- 변경 전: 항상 "+10%" (단계 증분값만 표시)
- 변경 후: `(currentLevel + 1) × valuePerLevel × 100` → 0레벨 선택 시 "+10%", 1레벨 선택 시 "+20%"

### 초기화 버튼

- `EnhanceUI.uxml`: `enhance-panel` 상단에 `btn-reset` 버튼 추가
- `EnhanceUI.uss`: 빨간 계열 버튼 스타일, `top: -34px; right: 0` 절대 위치
- 마우스 전용: `_btnReset.focusable = false` + `PointerUpEvent` 등록 (키보드 Enter/Space 무반응)
- `Debug.Assert(_btnReset != null)` — UXML 연결 누락 시 즉시 경고

### 대화창 버튼 레이블 시스템

**DialogueData.cs 확장**
- `string[] _confirmLabels`, `string[] _cancelLabels` — 줄별 버튼 텍스트 오버라이드 (Inspector에서 설정)
- `GetConfirmLabel(int)` / `GetCancelLabel(int)` → private `GetLabel(string[], int)` 헬퍼로 중복 통합

**DialogueUIController.cs 변경**
- `ShowCurrentLine()`: 선택지 줄이면 "예"/"아니오", 아니면 줄별 오버라이드 or 기본 "대화"/"취소"
- `ShowResponse()`: `EnhanceUIController.IsOpen`이면 Open()이 설정한 레이블 유지 (덮어쓰지 않음)
- `SetActionLabels(confirm, cancel)`: 외부에서 버튼 텍스트 일괄 변경 공개 API
- `ShowBeliefPanel(visible)`: `BeliefManager.OnBeliefChanged` 이벤트 `-=` 후 `+=` (중복 구독 방지)

### 토큰 게이지 UI

- `PlayerHUD.uxml`: 좌상단 `token-gauge-panel` 추가 (게이지 바 + "(0/3)" 카운터)
- `PlayerHUD.uss`: 금색 계열, `position: absolute; top: 14px; left: 14px`
- `PlayerHPUI.cs`: `TagTokenBank.OnGaugeChanged` 이벤트 구독 → `OnTokenGaugeChanged()` 콜백으로 갱신
  - 부분 채움 비율: `totalGauge % GaugePerToken / GaugePerToken`
  - 3/3 도달 시 바 초기화(0%), 카운터 "(3/3)" 유지
- `TagTokenBank.cs`: `GaugePerToken`, `MaxGauge` `private const` → `public const` 공개 + `Gain()`에 `Mathf.Approximately` no-op 가드 추가

### 최적화 (2회)

**1차**
- `EnhanceUIController`: 노드 DOM 참조 시작 시 캐싱, `_selectedNodeIndex` 도입, `"node-level"` → `NodeLevelClass` 상수
- `BeliefManager.OnValidate`: `Save()` → `OnBeliefChanged?.Invoke()` (인스펙터 수정 시 디스크 flush 방지)
- `DialogueData`: `GetConfirmLabel`/`GetCancelLabel` 중복 → `GetLabel` 헬퍼로 통합

**2차**
- `EnhanceSaveService`: `SetLevelNoFlush` + `FlushSave` 추가 → `ResetAllUpgrades` N번 flush → 1번
- `EnhanceUIController`: `ClearSelection()` 헬퍼 추출 (`HidePanel`/`ResetAllUpgrades`/`Open` 중복 제거)
- `EnhanceUIController`: `RefreshInfoPanel(def, level)` 오버로드 → `TryConfirmUpgrade`에서 `GetLevel` 중복 호출 제거
- `_btnReset` null 가드 → `Debug.Assert` 교체
- 불필요한 주석 제거 (`BeliefManager` 필드 주석, `/// <returns>`, `/// <summary>` 설명형 주석)

---

## 2026-05-12

### 대화창 버튼 마우스 클릭 지원 (`DialogueUIController.cs`)

- 대화/취소 버튼, 강화/나가기 버튼을 마우스로 클릭 가능하게 추가
- `Mouse.current` 직접 폴링 + `RuntimePanelUtils.ScreenToPanel()` + `worldBound.Contains()` 패턴
- UIToolkit Y축이 스크린 좌표계와 반대 → `new Vector2(screenPos.x, Screen.height - screenPos.y)`로 뒤집기
- 마우스 버튼 위에 있으면 hover 선택 (`SetSelection`) + 클릭 시 실행

### HUD UI 추가 (골드·신념 카운터)

**`Assets/UI/PlayerHUD.uxml`**
- `enemy-counter` 위에 `gold-counter`, `belief-counter` 요소 추가
- 구성: 아이콘(VisualElement) + 숫자 레이블

**`Assets/UI/PlayerHUD.uss`**
- `gold-counter` (bottom: 114px), `belief-counter` (bottom: 64px), `enemy-counter` (bottom: 14px) 50px 간격
- 골드 아이콘: `Assets/Screenshots/Coin.png`, 신념 아이콘: `Assets/Screenshots/신념_아이콘.png`

**`Assets/Scripts/UI/PlayerHPUI.cs`**
- `_goldCountLabel`, `_beliefCountLabel`, `_goldIcon` 필드 추가
- `GoldManager.OnGoldChanged` / `BeliefManager.OnBeliefChanged` 이벤트 구독 → 변경 시에만 갱신
- `GoldIconWorldPos` static 프로퍼티 + `CacheGoldIconWorldPos()` 코루틴
  - UIToolkit `worldBound` → Y 반전 → `Camera.ScreenToWorldPoint()` → 월드 좌표 변환
  - 코인 이펙트가 날아올 목표점으로 사용

### GoldManager 신규 생성 (`Assets/Scripts/Core/GoldManager.cs`)

- 메모리 전용 싱글턴 (PlayerPrefs 없음, 런 재시작 시 리셋)
- `DontDestroyOnLoad`, `OnGoldChanged` 이벤트, `AddGold()`, `TrySpendGold()`
- `StageManager.OnRunRestarted` 구독 → 재시작·클리어 후 자동 0 초기화

### 골드 체스트 (`Assets/Scripts/Interaction/GoldChest.cs`)

- `IInteractable` 구현, 플레이어 F키 상호작용 시 1회만 동작 (`CanInteract = !_opened`)
- `Animator.SetTrigger("Open")` → GoldChest.controller의 Open 트리거 발동
  - `GoldChest_Open.anim`: 4프레임 8fps, 마지막 프레임 정지 (루프 없음)
  - `GoldChest.controller`: Idle → Open 단방향 전환, HasExitTime=false
- 열리면 `SpawnCoinEffects()` 호출

### 코인 날아가는 이펙트

**`Assets/Scripts/Effect/CoinFlyEffect.cs`**
- `Play(startPos, target, goldAmount)` → 3단계 코루틴
  - Phase 1 (0.4초): 포물선 호 (물리 공식, gravity=-12, 랜덤 각도·속도)
  - Phase 2 (0.45~0.75초): 바닥 대기
  - Phase 3 (0.35초): smoothstep lerp로 골드 아이콘으로 수렴, 크기 축소
  - 도착 시 `GoldManager.AddGold()` 호출 → 카운터 코인마다 점진적으로 증가

**`Assets/Prefabs/CoinFlyEffect.prefab`**
- SpriteRenderer (`Coin.png`, sortingOrder: 10) + CoinFlyEffect 컴포넌트

**`GoldChest.cs` 코인 분배**
- `_goldAmount / count` + 나머지는 마지막 코인 → 총합 항상 일치

### 기사 Idle 스프라이트 피벗 수정 (`Assets/Sprites/Warrior/Idle.png.meta`)

**원인**: Idle(29×49px) vs Run(64×64px) 스프라이트 크기 차이로 중심 피벗 기준 발 위치가 7.5px 차이 → Idle에서 0.225 월드유닛 부유

**수정**: Idle 스프라이트 8개 모두 `alignment: 0(Center)` → `alignment: 9(Custom)` 변경
- 49px 높이 프레임: `pivot: {x: 0.5, y: 0.6531}` (32/49)
- 47px 높이 프레임 (Idle_6, Idle_7): `pivot: {x: 0.5, y: 0.6809}` (32/47)

### 최적화

**PlayerHUD.uss / UXML**
- `.counter-panel`, `.counter-icon`, `.counter-label` 공통 클래스 추출
- 세 카운터의 중복 CSS ~40줄 제거 (256줄 → 175줄)
- 고유 속성(bottom, color, image)만 `#id` 규칙으로 남김

**PlayerHPUI.cs**
- `_lastHp`, `_lastMaxHp`, `_lastEnemyCount` 캐싱 → HP·적 카운트 변경 시에만 텍스트 갱신
- `IPanel _panel` 캐싱 (`Start()`에서 1회)
- `SetSelection` early-out 추가 (같은 인덱스 재선택 시 스킵)

**GoldChest.cs**
- `_coinEffectPrefab` 타입 `GameObject` → `CoinFlyEffect` → `GetComponent<>()` 호출 제거
- `Instantiate()` 결과를 바로 `effect.Play()` 호출

**CoinFlyEffect.cs**
- `burstDuration`, `flyDuration` 지역변수 → `const BurstDuration`, `const FlyDuration` 클래스 상수

---

## 2026-05-15

### 코인 이펙트 Object Pool 적용

**신규 파일**

| 파일 | 역할 |
|------|------|
| `Assets/Scripts/Core/CoinPool.cs` | `ObjectPool<CoinFlyEffect>` 싱글턴. Inspector에서 pre-warm 수·최대 크기 설정 가능 |

**변경 파일**

| 파일 | 변경 내용 |
|------|-----------|
| `GoldChest.cs` | `Instantiate` → `CoinPool.Instance.Get()`, `_coinEffectPrefab` 필드 제거 |
| `CoinFlyEffect.cs` | `Destroy(gameObject)` → `CoinPool.Instance.Release(this)`, `Play()`에 `StopAllCoroutines()` + 스케일 초기화 추가 |

**설계 포인트**
- `createFunc`에서 `Instantiate(_coinPrefab, GoldManager.Instance.transform)` → 생성된 코인 Clone이 GoldManager 하위에 위치
- PreWarm을 `Awake` → `Start`로 이동 (GoldManager `Awake` 이후 실행 보장)
- `actionOnRelease`: `localScale = Vector3.one` + `SetActive(false)` 초기화

### 설정 창 음악 슬라이더 ↔ BGM 연동

| 파일 | 변경 내용 |
|------|-----------|
| `BGMManager.cs` | `SetVolume(float)` 추가 — `_volume` 갱신 + 현재 AudioSource 즉시 반영. 크로스페이드 중에도 코루틴이 `_volume` 참조해 자동 적용 |
| `SettingsMenuController.cs` | `SettingsSlider`에 `OnValueChanged` 이벤트 추가 (`RegisterValueChangedCallback` 기반, 마우스·키보드 모두 감지). `_rightSliders[1]`(음악) 변경 시 `BGMManager.SetVolume()` 호출. `Start()`에서 초기값으로 BGMManager 볼륨 초기화 |

### 최적화

| 파일 | 내용 |
|------|------|
| `CoinFlyEffect.cs` | `new WaitForSeconds(Random.Range(...))` → 수동 타이머 (GC 할당 제거), `HalfGravity = -6f` 상수 추출 |
| `GoldChest.cs` | `CoinPool.Instance`, `transform.position` 루프 전 지역 변수 캐싱 |
| `CoinPool.cs` | PreWarm 배열 2중 루프 → `Stack` 단일 패턴 |
| `BGMManager.cs` | `elapsed / _crossFadeDuration` 매 프레임 나눗셈 → `invDuration` 로컬 변수로 곱셈 대체, `Mathf.Lerp` → `_volume * t` / `_volume * (1f - t)` 단순화 |
| `SettingsMenuController.cs` | `SettingsSlider._lowValue` / `_highValue` 생성자에서 `readonly float` 캐싱 — 드래그 중 프로퍼티 반복 접근 제거 |

### 남은 사항
- 씬에 `CoinPool` GameObject 배치 후 `_coinPrefab`에 `CoinFlyEffect.prefab` 연결 필요
- 설정 창 효과음(sett-right-2) · 전체음량(sett-right-0) 슬라이더 연동 미구현

---

## 2026-05-18

### 노예상인 NPC 추가 (이전 세션 이월)

**신규 파일**

| 파일 | 역할 |
|------|------|
| `Assets/Scripts/NPC/NPCGoldEffect.cs` | `_onYesChosen`에 연결 — `GoldManager.AddGold()` 호출 |
| `Assets/Scripts/Core/Items/NpcSO/SlaveTrader_Dialogue.asset` | 노예상인 DialogueData SO (3줄 대사, HasChoice=true, 선행/악행 레이블) |
| `Assets/Sprites/SlaveTrader/SlaveTrader_Idle.anim` | 5프레임 8fps 루프 애니메이션 |
| `Assets/Sprites/SlaveTrader/SlaveTraderAnimator.controller` | Idle 단일 상태 Animator |
| `Assets/Prefabs/NPC_Prefab/SlaveTrader.prefab` | NPCController + NPCGoldEffect + BoxCollider2D(trigger) + FKeyPrompt |

- `StageShopRoot`에 SlaveTrader 인라인 배치 (position: -4, 1.187, 0)
- `_onYesChosen` → `NPCGoldEffect.Execute` 연결

### 파일 위치 변경으로 인한 노란 느낌표 수정 (USS 경로)

USS는 `url()` 경로 기반 참조 (GUID 무관) → 파일 이동 시 직접 경로 수정 필요

| 파일 | 수정 내용 |
|------|-----------|
| `PlayerHUD.uss` | `../Screenshots/Coin.png` → `../Screenshots/Assets/Coin.png` |
| `PlayerHUD.uss` | `../Screenshots/신념_아이콘.png` → `../Screenshots/Assets/신념_아이콘.png` |
| `DialogueUI.uss` | `../Screenshots/신념_아이콘.png` → `../Screenshots/Assets/신념_아이콘.png` |
| `EnhanceUI.uss` | `../Screenshots/신념_아이콘.png` → `../Screenshots/Assets/신념_아이콘.png` |
| `EnhanceUI.uss` | `../Screenshots/샵걸_이미지.png` → `../Screenshots/NPC/샵걸_이미지.png` |

- .meta 파일도 삭제된 것이 있어 `git show HEAD:<old_path>` 로 원본 GUID 복원

### 선행/악행 선택지 레이블 시스템

**설계**: NPC마다 버튼 레이블을 자유롭게 지정 (예/아니오, 선행/악행 등), 비워두면 기본값 사용

| 파일 | 변경 내용 |
|------|-----------|
| `DialogueData.cs` | `_choiceConfirmLabel`, `_choiceNoLabel` 필드 추가. 빈칸이면 "예"/"아니오" 반환 |
| `DialogueUIController.cs` | `ShowCurrentLine()`의 하드코딩된 "예"/"아니오" → `_currentData.ChoiceConfirmLabel` / `ChoiceNoLabel` 사용 |

- 노예상인 SO: `_choiceConfirmLabel = "선행"`, `_choiceNoLabel = "악행"` 설정
- 이후 추가될 선행/악행 NPC 5개에도 동일 방식 적용 가능

### NPC 재대화 기능

**동작**: HasChoice NPC에서 선택 완료 후 다시 말을 걸면 지정한 재대화 대사만 출력

| 파일 | 변경 내용 |
|------|-----------|
| `DialogueData.cs` | `_repeatDialogue` (매번 전체 대화 재출력 토글), `_retalkLine` (재대화 전용 대사), `InitSingleLine()` 메서드 추가 |
| `NPCController.cs` | `RecordChoice()` — 선택 시 1회만 `DialogueData.CreateInstance` 생성 후 `_retalkData` 필드에 저장. `OnDestroy()`에서 해제. `OnInteract()`에서 `_hasChosen` 분기 |

- `_repeatDialogue = true` 이면 선택 후에도 전체 대화 반복 (매 상호작용 마다)
- `_retalkLine` 비워두면 재대화 시 빈 텍스트 표시 (SO에서 반드시 입력 권장)
- HasChoice 없는 NPC는 완전히 영향 없음

**설계 근거**: `StartSingleLine` API 신설보다 `DialogueData.CreateInstance` 1회 저장 방식이 `DialogueUIController` 수정 없이 기존 대화 흐름 재활용 가능 — 변경 파일 최소화

### 버그 수정 — 아래키+점프 시 몬스터도 낙하

**원인**: `DropThroughPlatform()`에서 `col.isTrigger = true` 로 플랫폼 자체를 트리거로 전환 → 해당 플랫폼 위의 모든 오브젝트(몬스터 포함) 충돌 무효화

**수정** (`PlayerController.cs`):
- `isTrigger` 전환 방식 → `Physics2D.IgnoreCollision(플레이어콜라이더, 플랫폼콜라이더, true/false)` 방식
- 0.4초 후 충돌 복원 (기존 대기 시간 유지)
- `_ownColliders = GetComponents<Collider2D>()` — `Awake()`에서 캐싱, 코루틴 내 `GetComponents` 호출 제거

### 최적화

| 파일 | 내용 |
|------|------|
| `NPCController.cs` | `RecordChoice(bool yes)` → `RecordChoice()` — 폴백 제거 후 미사용 파라미터 제거 |
| `DialogueData.cs` | 프로퍼티 선언 순서 재정렬 (NpcName→Lines→HasChoice→RepeatDialogue→YesResponse→NoResponse→RetalkLine) |

---

## 2026-05-19

### Stage1Root NPC 4종 배치

**신규 프리팹**

| 파일 | 역할 |
|------|------|
| `Assets/Prefabs/NPC_Prefab/BuffNPC.prefab` | 버프 NPC. NPCController + BoxCollider2D(trigger) + FKeyPrompt 자식 |
| `Assets/Prefabs/NPC_Prefab/VeteranVigilante.prefab` | 베테랑 자경단원 NPC. 동일 구조 |
| `Assets/Prefabs/NPC_Prefab/HolyKnight.prefab` | 성기사 NPC. 동일 구조 |
| `Assets/Prefabs/NPC_Prefab/Bard.prefab` | 음유시인 NPC. 동일 구조 |

**배치 위치 (Stage1Root 기준)**
- BuffNPC: (-10, 1.5, 0)
- VeteranVigilante: (-4, 1.5, 0)
- HolyKnight: (4, 1.5, 0)
- Bard: (10, 1.5, 0)

**구현 방식**
- `manage_prefabs.create_child`는 빈 GameObject만 생성 → 프리팹 인스턴스 불가
- `Assets/Editor/NpcPlacer.cs` 임시 에디터 스크립트 작성: `PrefabUtility.LoadPrefabContents` + `PrefabUtility.InstantiatePrefab` + `PrefabUtility.SaveAsPrefabAsset` 패턴으로 해결
- Tools > Place Stage1 NPCs 실행 후 NpcPlacer.cs 삭제

**남은 사항**
- 4개 NPC DialogueData SO 대사 내용 Inspector에서 직접 입력 필요 (현재 "..." 플레이스홀더)
- BuffNPC `_onYesChosen`/_onNoChosen UnityEvent 연결 — 버프 효과 결정 후 Inspector에서 설정

### 타일맵 시스템 구현 (Stage1 배경)

**신규 에셋**
- `Assets/Tiles/Stage1/1스테이지 타일맵_0.asset` ~ `_38.asset` — 39개 Tile 에셋 (1스테이지 타일맵.png 슬라이스 기반)
- `Assets/Tiles/Stage1/Stage1Palette.prefab` — Tile Palette (6열 배치)

**Stage1Root 구조**
```
TilemapBackground (Grid, cellSize=0.54×0.47)
├── Tilemap_Ground   — TilemapCollider2D(Merge) + Rigidbody2D(Static) + CompositeCollider2D, Layer 9, sortingOrder=5
└── Tilemap_Platform — TilemapCollider2D(UsedByEffector) + PlatformEffector2D, Layer 10, sortingOrder=4
```

**타일맵 페인팅 방법**
- `Window > 2D > Tile Palette` → Stage1Palette 선택
- Active Tilemap을 `Tilemap_Ground` 또는 `Tilemap_Platform`으로 전환하며 씬 뷰에서 직접 그리기

**해결한 문제들**

| 문제 | 원인 | 해결 |
|------|------|------|
| Tile Palette 드롭다운에 안 뜸 | 프리팹에 `GridPalette` 서브에셋 없음 | `AssetDatabase.AddObjectToAsset`으로 추가 |
| 플레이어가 타일을 통과 | 타일 에셋 `m_ColliderType: 0` (None) | 39개 전부 `2` (Grid)로 일괄 변경 |
| 타일 경계마다 보이지 않는 벽 | 타일별 개별 콜라이더 → 모서리 걸림 | `CompositeCollider2D`로 병합 |

### NPC 스크립트 최적화

| 파일 | 내용 |
|------|------|
| `NPCController.cs` | 다중 줄 `/// <summary>` 블록 제거 |
| `NPCHealEffect.cs` | summary 블록 제거, `FindObjectOfType` → `FindFirstObjectByType` (Unity 6 deprecated API 수정) |
| `NPCGoldEffect.cs` | summary 블록 제거 |
| `DialogueData.cs` | `InitSingleLine` summary 블록 제거 |

### BackGround_Sec 레이어 정리

- `지옥 배경_0` SpriteRenderer sortingOrder: 0 → **-100** (가장 뒤로)
- `나무_0` ~ `나무_5` SpriteRenderer sortingOrder: 원래 0 그대로 유지 (실수로 -100이 됐다가 복구)
- InGameScene 저장 완료

---

## 2026-05-25

### 패럴랙스 배경 시스템 구현

**신규 파일**

| 파일 | 역할 |
|------|------|
| `Assets/Scripts/Floor/ParallaxLayer.cs` | X 무한반복 + X·Y 패럴랙스 이동 컴포넌트 |

**씬 구조 (`##--BACKGROUND--##` 하위)**

```
##--BACKGROUND--##
  ├── BG_Far_Mid   ← ParallaxLayer + SpriteRenderer (sortingOrder: -10) — 중앙 원본
  ├── BG_Far_Left  ← SpriteRenderer (_leftCopy 참조됨)
  └── BG_Far_Right ← SpriteRenderer (_rightCopy 참조됨)
```

**구현 내용**

- **X 무한 반복**: 씬에 3장(좌·중·우) 미리 배치, 중앙이 카메라에서 spriteWidth 이상 벗어나면 `_startPos.x` 조정으로 seamless 재배치
- **Y 패럴랙스**: 복사본 없이 이동량만 적용 (수직 이동 범위가 제한적이므로)
- **절대 위치 방식**: `배경 위치 = 시작위치 + 카메라총이동량 × (1 - factor)` — delta 누적 오차 없음, 실행 순서 무관
- **Factor 인터페이스**: 작을수록 느림(뒷배경), 클수록 빠름(앞배경), 0 = 카메라와 완전히 동행

**버그 수정 이력**

| 문제 | 원인 | 수정 |
|------|------|------|
| 배경이 전혀 안 움직임 | `CameraFollow`와 `ParallaxLayer` 모두 `LateUpdate` — 실행 순서 미보장으로 delta = 0 | `[DefaultExecutionOrder(-10)]` / `[DefaultExecutionOrder(10)]` 로 순서 고정 |
| 배경이 카메라와 동일 속도로 움직임 | delta 누적 방식은 실행 순서 1프레임 차이에도 오차 발생 | 절대 위치 계산 방식으로 전면 교체 |
| factor 0.1이 빠르고 0.9가 느림 (직관 반대) | `camOffset * factor` 공식에서 factor가 작을수록 배경이 세계좌표 이동량이 적어 카메라 기준 빠르게 스크롤 | `camOffset * (1 - factor)` 로 반전 |

**최적화**

| 항목 | 내용 |
|------|------|
| `_cameraTransform` 캐싱 | `Camera.main.transform`을 `Start()`에서 1회 캐싱 — 매 프레임 `.transform` 접근 제거 |
| `_mulX` / `_mulY` 미리 계산 | `(1 - factor)` 연산을 `Start()`에서 수행, `OnValidate()`로 Inspector 변경 시 즉시 반영 |
| `_hasInfiniteScroll` 플래그 | null 체크 3회 → bool 1회 조회로 단순화 |
| `targetX` 재활용 | `transform.position.x` 재접근 없이 이미 계산된 값 사용 |

---

## 2026-05-27

### 보스 팔(Boss_Min_Right) 미활성화 버그 수정

**현상**
- StageBossRoot가 스테이지 전환으로 활성화될 때 Boss_Min_Left는 나타나지만 Boss_Min_Right만 활성화되지 않음

**원인 분석**
1. `BossFistPattern.Awake()`에서 `_rightFist?.Hide()`(= `SetActive(false)`) 호출
2. Boss_Min_Right가 비활성화된 상태에서는 Unity가 `FistProjectile.Awake()`를 실행하지 않음
3. → `_collider` 필드가 null 상태로 남아 이후 활성화 시도 시 오류 발생
4. 추가로 오른팔은 `_rightArmOffset = 6f` 오프셋이 있어 보스 교전 후 6초 뒤에야 루프 시작 — 양 팔이 동시에 보이지 않는 UX 문제도 있었음

**수정 내용**

| 파일 | 변경 내용 |
|------|-----------|
| `FistProjectile.cs` | `_collider.isTrigger = true` 및 `_collider.enabled = false` 초기화를 `Awake()` → `OnEnable()`로 이동 (활성화될 때마다 실행 보장) |
| `FistProjectile.cs` | `ShowAtSpawn(Vector3)` 메서드 추가 — 회전 변경 없이 스폰 위치에서 콜라이더 OFF 상태로 표시 |
| `FistProjectile.cs` | 미사용 `_renderer(SpriteRenderer)` 필드 및 Awake GetComponent 제거 |
| `BossFistPattern.cs` | `Awake()`의 `_leftFist?.Hide()` / `_rightFist?.Hide()` 제거 |
| `BossFistPattern.cs` | `HandleBossEngaged()`에서 양 팔 모두 `ShowAtSpawn()` 호출 후 `BeginExecute()` — 보스 교전 시 양 팔이 동시에 스폰 위치에 나타남 (첫 진입·재진입 모두 동작) |

### 보스 관련 코드 최적화

| 파일 | 최적화 내용 |
|------|-------------|
| `FistProjectile.cs` | `MoveRoutine()` 내 `Time.deltaTime`을 `float dt`로 캐싱 — 매 프레임 네이티브 호출 1회로 감소 |
| `BossFistPattern.cs` | `FistLoop()`에서 `spawnPoint.position`을 루프 전 `Vector3 spawnPos`로 캐싱 — Transform 네이티브 호출 반복 제거 |
| `BossCustomPatternBase.cs` | `PauseableWait()`에서 `_pauseCtrl != null` null 체크를 `bool hasPauseCtrl`로 루프 밖에 캐싱 |
| `BossFirePillarPattern.cs` | `GeneratePositions()` 내부 중복 검사를 `foreach` → 인덱스 `for` 루프로 교체 (List enumerator GC 할당 제거) |
| `FirePillarActor.cs` | 경고 페이즈 루프에서 `elapsed / _warningDuration` 매 프레임 나눗셈 → `invWarningDur = 1f / _warningDuration` 곱셈으로 교체 |

### 보스 체력바 HUD 구현 (BossHUD)

**신규 파일**

| 파일 | 역할 |
|------|------|
| `Assets/UI/BossHUD.uxml` | 보스명 / 젬+HP바 / 종족명 레이아웃 |
| `Assets/UI/BossHUD.uss` | 어두운 패널·황금 테두리·보라 젬·빨간 HP바 스타일 |
| `Assets/UI/BossHUDPanelSettings.asset` | HUDPanelSettings 복제, sortingOrder: 5 |
| `Assets/Scripts/UI/BossHUDController.cs` | 슬라이드 인/아웃 + HP 갱신 컨트롤러 |

**씬**: InGameScene에 `BossHUD` GameObject (UIDocument + BossHUDController) 추가

**동작**
- 기본 상태: 화면 위쪽 밖(-200px)에 숨겨진 상태
- `BossMainController.OnBossEngaged` 또는 `BossController.OnBossEngaged` 발생 → 위에서 SmoothStep 슬라이드 인 (0.55초)
- 보스 사망 → 슬라이드 아웃 후 참조 정리
- HP 소스 자동 감지: `BossController` 보스 → `BossStats.HpRatio` 직접 사용, `BossMainController` 보스 → `BossPartHP[]` 합산 비율 사용
- 텍스트 하드코딩: 보스명 `"위그드라실"`, 종족 `"장로엔트"` (UXML에서 직접 수정)

**최적화**
- `_bossActive` bool 플래그 → Update 가드 (null 체크 2개 → bool 1개)
- `_lastHpRatio` 캐시 → HP 변화 없을 때 UIToolkit `style.width` write 스킵
- `CacheBossHpSource()` 분리 → `HandleBossEngaged` 단일 책임
- SlideCoroutine 임시 변수 `t` 제거 (SmoothStep 인라인)

---

## 2026-05-30

### 몬스터 ScriptableObject 시스템 구축

**CS 타입 정의 파일 (`Assets/Scripts/Enemy/MonsterSO/`)**

| 파일 | 대상 |
|------|------|
| `EnemyDataSO.cs` | 공통 기반 (abstract) — maxHp, moveSpeed, 감지 범위, 피격 사운드 등 |
| `MeleeEnemyDataSO.cs` | 근접 적 (attackDamage, knockbackForce) |
| `RangedEnemyDataSO.cs` | 원거리 적 (+ windupDuration, projectilePrefab) |
| `EmissionEnemyDataSO.cs` | 다방향 원거리 적 (+ projectileCount, fireInterval) |
| `MidBossDataSO.cs` | 미드보스 (+ 슬램 스킬 전체 파라미터) |
| `BossStatsSO.cs` | 보스 체력 (maxHp, phase2HpRatio) |
| `BossArcherDataSO.cs` | 궁수 보스 이동·패턴 전체 |
| `BossMainDataSO.cs` | 메인보스 파츠 시스템 |
| `BossPatternDataSO.cs` | 메인보스 패턴 데이터 |
| `BossControllerDataSO.cs` | 메인보스 컨트롤러 전환 연출 |
| `BossPartHPDataSO.cs` | 보스 파츠 개별 체력 |
| `BossBreathEffectDataSO.cs` | 보스 호흡 효과 |
| `BossFirePillarDataSO.cs` | 불기둥 패턴 데이터 |
| `BossFistPatternDataSO.cs` | 주먹 패턴 타이밍 |
| `FistProjectileDataSO.cs` | 주먹 발사체 스탯 |

**SO 에셋 파일 생성 및 프리팹 연결**

| 에셋 | 연결 대상 |
|------|-----------|
| `Orc_Data.asset` | Enemy_Orc.prefab |
| `Archer_Data.asset` | Enemy_Archer.prefab |
| `SkeletonMage_Data.asset` | Enemy_SkeletonMage.prefab |
| `Wizard_Data.asset` | Enemy_Wizard.prefab |
| `MidBoss_Data.asset` | Enemy_MidBoss.prefab |
| `ArcherBoss_Stats/Data.asset` | ArcherBoss.prefab |
| `MainBoss_Stats/PatternData/ControllerData.asset` | Boss.prefab (메인보스) |
| `MainBoss_Data.asset` | StageBossRoot BossMainController |
| `MainBoss_2_Data.asset` | StageBossRoot2 BossMainController |
| `Boss2_Body/Left/Right/Head_HP.asset` | StageBossRoot2 파츠별 BossPartHP |
| `Boss2_BreathEffect/FirePillar/FistPattern/FistProjectile.asset` | StageBossRoot2 각 컴포넌트 |

**연결 방식**: 각 컴포넌트 `Awake()`에서 SO null 체크 후 적용 — SO 미연결 시 Inspector 기본값 유지 (하위 호환)

**수정된 컴포넌트**: `EnemyStats`, `EnemyBrainBase`(+파생), `BossStats`, `BossArcherController`, `BossMainController`, `BossPatternExecutor`, `BossController`, `BossPartHP`, `BossBreathEffect`, `BossFirePillarPattern`, `BossFistPattern`, `FistProjectile`

---

### FistProjectile 조준 레이저 구현

- **각도 보정**: 주먹 스프라이트가 Z=0일 때 +Y 방향을 보므로 `Atan2(y,x) * Rad2Deg - 90f` 적용
- **LineRenderer 방식**: 조준 중 빨간 레이저 표시 (`startColor: (1,0,0,0.85)`, `endColor: (1,0.1,0.1,0.25)`)
- **URP 머티리얼**: `FistLaserMat.mat` (Particles/Unlit) 생성 → LineRenderer 핑크 문제 해결
- **포효 시 숨김**: `BossFistPattern.FistLoop()`에서 `IsBossFrozen` 체크 → `SetLaserVisible(false)` 호출
- **스프라이트 시도 → 복귀**: `레이저.png` 스프라이트로 전환 시도 후 LineRenderer 방식으로 최종 복귀

---

### 코드 최적화

| 파일 | 내용 |
|------|------|
| `EnemyStats.cs` | `SafeSetTrigger()`: `animator.parameters` 매 호출 O(n) 순회 → `HashSet<int>` Awake 캐싱으로 O(1). `Debug.Log` 2개 제거 |
| `PlayerController.cs` | `DropThroughPlatform()`: `OverlapBoxAll`(GC 매 호출 할당) → `OverlapBoxNonAlloc` + `_platformBuffer` 인스턴스 필드로 사전 할당 |
| `PlayerDash.cs` | 중력 복원 하드코딩 `gravityScale = 3` → `_originalGravityScale` (버그 수정 겸 최적화) |
| `PlayerStats.cs` | 핫패스 `Debug.Log` 제거 |
| `StatusController.cs` | `Update` / `OnHitReceived` 중복 remove 로직 → `RemoveFinished()` 추출. `_toRemove` List 인스턴스 재사용으로 GC 할당 제거 |


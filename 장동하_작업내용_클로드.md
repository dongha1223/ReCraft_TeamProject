# 장동하 작업내용 (Claude 정리)

대화 종료 시점에 Claude가 직접 요약·누적 기록합니다.

---

## 2026-03-30

### 설정
- 대화 요약 파일 생성 (`장동하_작업내용_클로드.md`)
- 매 대화 마무리 시 Claude가 이 파일에 내용을 누적 정리하기로 결정

### 시작화면 구현
- `SampleScene` → `InGameScene` 이름 변경 (이미 `Assets/InGameScene.unity`로 존재)
- `Assets/Scenes/StartScene.unity` 씬 생성, 빌드 세팅 등록 (StartScene:0, InGameScene:1)
- `Assets/UI/StartScreen.uxml` / `StartScreen.uss` 생성 (UIToolkit 기반)
- `Assets/Scripts/UI/StartSceneController.cs` 생성
  - 검은화면 Fade In → 로고 이미지 + "아무 키나 입력하세요" 점멸
  - 키보드 / 마우스 좌클릭 입력 감지
  - Fade Out → InGameScene 씬 전환
- `Assets/Screenshots/Logo.jpg` 이미지를 로고로 사용 (USS 상대경로 `../Screenshots/Logo.jpg`)
- `Assets/UI/StartScreenPanelSettings.asset` 생성 (HUDPanelSettings 복제)

### 남은 사항
- InGameScene 진입 시 Fade In 효과 (미구현, 필요 시 추가)
- 로고를 이미지 파일 대신 직접 제작한 에셋으로 교체 예정

---

## 2026-03-31

### 시작화면 보완
- 로고 이미지(`Assets/Screenshots/Logo.jpg`)를 화면 전체 배경으로 변경
  - `logo-image` VisualElement를 content-root 밖으로 분리, position absolute 전체화면
  - USS: `stretch-to-fill`로 화면 꽉 채움
- "아무 키나 입력하세요" 위치를 화면 하단 중앙으로 변경 (bottom: 80px)
- 마우스 좌클릭 입력 감지 추가 (`Mouse.current.leftButton.wasPressedThisFrame`)

### 일시정지 메뉴 구현
- `Assets/UI/PauseMenu.uxml` / `PauseMenu.uss` 생성
  - 반투명 오버레이 + 어두운 보라색 패널 + 해골(☠) + "일시정지" 타이틀
  - 메뉴 5개: 돌아가기, 새 게임, 컨트롤, 설정, 게임종료
  - 선택 항목 강조: `menu-item--selected` CSS 클래스 동적 적용
- `Assets/Scripts/UI/PauseMenuController.cs` 생성
  - ESC: 메뉴 열기/닫기 + `Time.timeScale` 0/1 제어
  - ↑↓ 화살표: 순환 네비게이션 (`% MenuCount`)
  - Enter/Space: 선택 실행
  - 게임종료만 기능 구현 (`Application.Quit` / 에디터는 `isPlaying = false`)
- InGameScene에 `PauseMenuController` GameObject 배치 완료

### 남은 사항
- 새 게임 / 컨트롤 / 설정 기능 미구현 (빈 케이스로 대기 중)
- InGameScene 진입 시 Fade In 효과 미구현

---

## 2026-04-01

### 3스테이지 시스템 구현 (단일 씬 방식)

**구조 설계**
- 씬을 새로 만드는 대신 InGameScene 안에서 `##--STAGES--##` 부모 오브젝트 아래 Stage1Root / Stage2Root / Stage3Root를 두고 SetActive로 전환하는 방식 채택
- Stage2, Stage3는 에디터에서 비활성 상태로 시작

**스테이지 구성**
- Stage 1: NPC존 (적 없음, 표지판만 존재 — 추후 상점/강화 공간 예정)
- Stage 2: 기존 맵 (오크 + 마법사 적 혼합)
- Stage 3: 신규 맵 (Stage 2 지형 복제 기반, 오크 + 마법사 혼합)

**생성한 파일**

| 파일 | 역할 |
|------|------|
| `Assets/Scripts/Core/StageManager.cs` | 스테이지 전환·적 카운트 총괄 싱글턴 |
| `Assets/Scripts/Core/FadeManager.cs` | UIToolkit 기반 FadeIn/FadeOut·게임클리어 UI |
| `Assets/Scripts/Stage/SignpostController.cs` | 표지판 상호작용 (알파·F키·이벤트 구독) |
| `Assets/Scripts/Stage/EnemySpawner.cs` | 스테이지 활성화 시 적 등록 및 재방문 복구 |
| `Assets/UI/GameClearUI.uxml` / `.uss` | GAME CLEAR 오버레이 + 카운트다운 레이블 |

**핵심 로직**
- `EnemySpawner.OnEnable`: 스테이지 루트 활성화 시 자동 호출 → 하위 EnemyStats 전부 찾아 `ResetStats()` + `RegisterEnemy()`
- `EnemyStats.OnDead()`: 사망 시 `StageManager.OnEnemyDied()` 호출 → 카운터 감소
- `StageManager.ActivateStage()`: `_aliveEnemyCount = 0` 초기화를 `SetActive(true)` **전**에 실행 (순서 중요)
- `CheckAllEnemiesDeadNextFrame()`: SetActive 직후 한 프레임 대기 후 AllEnemiesDead 검사 → 이벤트 발행
- `SignpostController`: `OnAllEnemiesDead` 이벤트 구독 → 수신 시 알파 50→255, F키 프롬프트 활성화

**게임 클리어 흐름**
- Stage 3 표지판 `_isLastStage = true` 설정
- F키 → `TriggerGameClear()` → FadeOut → GAME CLEAR 표시 → FadeIn → 10초 카운트다운 → FadeOut → Stage 1 복귀 → FadeIn

**해결한 버그**
1. 게임 시작 시 Stage 3에서 시작하는 버그
   → `GameObject.Find()`는 비활성 오브젝트를 탐색 못 함. Stage2·3를 에디터에서 비활성화하여 해결
2. 표지판이 적 처치 전에 활성화되는 버그
   → `CheckAllEnemiesDeadNextFrame` 코루틴으로 다음 프레임에 검사하도록 수정
3. Stage 1→2 전환 시 오류 (UnassignedReferenceException)
   → Inspector에서 직접 할당 후 해결, 불필요한 자동 탐색 코드 전부 제거

### 몬스터 카운터 UI 추가

- `PlayerHUD.uxml`에 `enemy-counter` 요소 추가 (☠ 레이블 + 숫자 레이블)
- `PlayerHUD.uss`에 스타일 추가 — 미니맵 왼쪽(right: 210px, bottom: 20px), 반투명 패널, 해골 빨간색/숫자 흰색
- `StageManager.cs`에 `public int AliveEnemyCount` 프로퍼티 추가
- `PlayerHPUI.cs`에 `UpdateEnemyCount()` 추가 — 매 프레임 `StageManager.AliveEnemyCount`를 읽어 레이블 갱신

### 남은 사항
- Stage 1 NPC존 콘텐츠 미구현 (상점·강화 등 추후 추가 예정)

---
